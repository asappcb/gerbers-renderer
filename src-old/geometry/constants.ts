// src/geometry/constants.ts

/**
 * Default board dimensions in millimeters used when we cannot infer
 * an outline from the Gerbers yet.
 */
export const DEFAULT_BOARD_WIDTH_MM = 100;
export const DEFAULT_BOARD_HEIGHT_MM = 50;
